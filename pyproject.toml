[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ggarch"
version = "0.1.0"
description = "Grammar of Architecture Diagrams"
license = { text = "Apache-2.0" }
requires-python = ">=3.11"
dependencies = [
    "lark>=1.1",
    "kiwisolver>=1.4",
    "drawsvg>=2.0",
]

[project.optional-dependencies]
sphinx = ["sphinx>=7.0"]
dev = [
    "pytest>=8.0",
    "pytest-cov",
    "sphinx>=7.0",
]

[project.scripts]
ggarch = "ggarch.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

# grammar.lark ships inside the package — the parser loads it via
# importlib.resources; without this, a non-editable install (CI) has
# no grammar and every parse fails (caught by the CI dry-run).
[tool.setuptools.package-data]
ggarch = ["grammar.lark"]

[tool.pytest.ini_options]
testpaths = ["tests"]
